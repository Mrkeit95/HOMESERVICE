// Single source of truth for the product's brand identity.
// The name "Doora" is a working placeholder — the .com is taken, so this will
// change. Update it HERE and it propagates everywhere (logo, title, copy).
export const BRAND = {
  name: 'Doora',
  // The accent dot/styling after the name in the logo.
  dot: '.',
  tagline: 'Home Services, On Demand',
  location: 'Canggu',
} as const;
